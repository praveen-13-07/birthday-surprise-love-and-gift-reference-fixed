Drop the background track here (e.g. song.mp3).
It will be loaded only after a user interaction (per the no-autoplay rule) —
wired up when the audio control is built. Referenced by path from
`src/data/content.js`, so swapping the track is a one-line change.
