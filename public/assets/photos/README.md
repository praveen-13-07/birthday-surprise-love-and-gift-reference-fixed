# Birthday photos

The 11 supplied images are included as `photo-01.jpg` through `photo-11.jpg`.
A few source images were rotated to the natural upright direction before being optimized for the website.
Replace any file with another image using the same filename if you want to swap a memory later.
